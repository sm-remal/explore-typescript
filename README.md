# 📘 TypeScript Learning Notes

এই রিপোজিটরিতে আমার TypeScript শেখার বেসিক নোটগুলো সংরক্ষণ করা হয়েছে।
এখানে TypeScript ইনস্টলেশন, কমান্ড, ডাটা টাইপ এবং গুরুত্বপূর্ণ
কনসেপ্টগুলো সংক্ষেপে তুলে ধরা হয়েছে।

------------------------------------------------------------------------

## 🚀 TypeScript Installation

গ্লোবালি TypeScript ইনস্টল করতে:

``` bash
npm install -g typescript
```

ইনস্টল হয়েছে কিনা চেক করতে:

``` bash
tsc --v
```

------------------------------------------------------------------------

## ⚙️ TypeScript Compile & Run

একটি নির্দিষ্ট ফাইল কম্পাইল করতে:

``` bash
tsc main.ts
```

ফাইল পরিবর্তন হলে অটো কম্পাইল (watch mode):

``` bash
tsc main.ts --w
```

Node দিয়ে রান করতে:

``` bash
node main.js
```

> ⚠️ Note: TypeScript ফাইল (`.ts`) সরাসরি Node দিয়ে রান করা যায় না। আগে
> `.js` এ কম্পাইল করতে হবে।

------------------------------------------------------------------------

## 🛠️ TypeScript Configuration

`tsconfig.json` ফাইল তৈরি করতে:

``` bash
tsc --init
```

সব ফাইল watch mode এ চালাতে:

``` bash
tsc -w
```

------------------------------------------------------------------------

## 📚 TypeScript Data Types

1.  String – টেক্সট ভ্যালু রাখার জন্য
2.  Number – নাম্বার ভ্যালু
3.  Boolean – true / false
4.  Any – যেকোনো টাইপ (avoid করা ভালো)
5.  Union – একাধিক টাইপ একসাথে ব্যবহার করা
6.  Void – কিছু return না করলে
7.  Never – কখনোই return করবে না
8.  Tuples – নির্দিষ্ট লেন্থ ও টাইপের array
9.  Enum – নির্দিষ্ট কিছু কনস্ট্যান্ট ভ্যালু
10. Narrowing Type – টাইপ চেক করে নির্দিষ্ট টাইপে কনভার্ট করা

------------------------------------------------------------------------

## 🔎 void vs never

### ✅ void

যখন একটি function কিছু return করবে না, তখন `void` ব্যবহার করা হয়।

``` ts
function sayHello(): void {
  console.log("Hello TypeScript");
}
```

### 🔥 never

যখন একটি function কখনোই কোনো ভ্যালু return করবে না (যেমন error throw
করবে বা infinite loop চলবে), তখন `never` ব্যবহার করা হয়।

``` ts
function throwError(): never {
  throw new Error("Something went wrong!");
}
```

------------------------------------------------------------------------

✨ Learning never stops. Practice regularly to become confident in
TypeScript!
